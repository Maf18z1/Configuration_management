#!/bin/bash

# Ищем все файлы с заданным расширением в текущем каталоге и сохраняем их в массиве
files=( $(find . -type f -name "*.$1") )

# Создаем архив файлов с указанным расширением
tar -cvf "archive.tar" "${files[@]}"

echo "Архив создан"
