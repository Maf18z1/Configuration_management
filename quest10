#!/bin/bash

# Итерируемся по всем файлам в директории
for file in "$1"/*; do

    # Если найден файл и данный файл имеет размер 0
    if [[ -f "$file" && ! -s "$file" ]]; then
        echo "Пустой файл: $file"
    fi
done
