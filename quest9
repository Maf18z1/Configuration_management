#!/bin/bash

# sed s/ что_заменять /на_что_заменять /опции
# \t - табуляция
# g - Замените все вхождения в файле
sed 's/    /\t/g' "$1" > "$2"

# Выводим сообщение об успешном изменении
echo "Файл исправлен"
